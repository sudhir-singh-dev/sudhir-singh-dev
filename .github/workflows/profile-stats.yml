name: Update Profile Stats

on:
  workflow_dispatch:
  schedule:
    - cron: "17 3 * * *"

permissions:
  contents: write

concurrency:
  group: update-profile-stats
  cancel-in-progress: false

jobs:
  update:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Generate profile stats
        uses: shinpr/github-profile-stats@49b5f7091182a45f3ef93923505b660c6da5f835 # v0.2.0
        id: stats
        with:
          username: ${{ github.repository_owner }}
          token: ${{ github.token }}
          profile: signal-field

      - name: Stage generated card
        env:
          READY_DIR: ${{ steps.stats.outputs.ready-dir }}
        run: |
          mkdir -p assets/profile
          cp "$READY_DIR"/signal-field-*.svg assets/profile/

          test -s assets/profile/signal-field-wide-light.svg
          test -s assets/profile/signal-field-wide-dark.svg
          test -s assets/profile/signal-field-compact-light.svg
          test -s assets/profile/signal-field-compact-dark.svg

      - name: Commit generated cards
        run: |
          git add assets/profile/signal-field-*.svg

          if git diff --cached --quiet; then
            echo "No profile-stat changes."
            exit 0
          fi

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          git commit -m "Update profile stats"
          git push
